# Rename this file to `credentials.json` and fill the values below

discord:
  # Bot access token
  token: 
  # Test guild ID. If not specified - bot registers commands globally
  guild: 
  # Remove all commands after shutdowning or not
  removeCommands: true

# OpenAI access token
openAIAPIKey: 
